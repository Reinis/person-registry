{% extends "base.twig" %}

{% block title %}Login{% endblock %}

{% block content %}
    {{ parent() }}
    <div class="flex justify-center">
        <form action="/login" method="post" class="mb-4">
            <div class="flex flex-col col-span-4 text-xl">
                <label for="nid">National Id</label>
                <input type="text" id="nid" placeholder="000000-00000" name="nid" class="rounded"
                       {% if nid %}value="{{ nid }}"{% endif %}>
            </div>
            <div class="flex justify-between items-center mt-4">
                <a href="/" class="w-24 px-4 py-1 rounded bg-green-400 text-center text-lg">Back</a>
                <input type="submit"
                       class="w-24 px-4 py-1 rounded bg-green-400 text-center font-bold text-lg cursor-pointer"
                       value="Login">
            </div>
        </form>
    </div>
    {% if token %}
        <div class="flex justify-center mt-8">
            <a href="/otp?token={{ token.token }}">Authenticate with National Id {{ token.nid }}</a>
        </div>
    {% endif %}
{% endblock %}
